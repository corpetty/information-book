data/nodes.jsonl
data/edges.jsonl
data/build-stats.json
data/claim-candidates.jsonl
data/extraction-catalog.json
data/interpretive-triples.jsonl
data/interpretive-notes.json
node_modules/
*.log
.DS_Store

# Obsidian config (user-specific)
.obsidian/

# Claude Code per-user settings (launch.json is committed as a shared file)
.claude/settings.local.json

# Quartz site build output and caches
site/public/
site/.quartz-cache/

# Local-only staging for in-copyright source material (books, etc.) —
# read at extraction time, never committed or published.
sources-local/
